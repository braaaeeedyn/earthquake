# Keep shell scripts and Linux deploy configs as LF, even on Windows checkouts, so they run
# correctly on the Linux host (CRLF breaks bash shebangs and can trip systemd/Caddy).
*.sh            text eol=lf
*.service       text eol=lf
deploy/Caddyfile text eol=lf
